// Tailwind CSS v4 configuration
// Most configuration is now done in CSS using @theme

export default {
  // Minimal config for Tailwind 4
  // Theme customization should be done in CSS files using @theme
}
